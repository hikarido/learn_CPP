Простой фреймворк для unit тестирования по задаче от coursera
С++ желтый пояс

#Содержание:
Небольшой фреймворк для тестирования

#Структура:

.
├── CMakeLists.txt          файл сборки
├── main.cpp                пример использования
├── printFramework.h        обеспечивает вывод структур данных на экран в удобном формате
├── README                  данная инструкция
└── simpleTestFramework.h   предоставляет функции для удобного тестирования, использует printFramework

