Простой фреймворк для unit тестирования по задаче от coursera
С++ желтый пояс

#Содержание:
Небольшой фреймворк дл тестирования

#Структура:

.
├── CMakeLists.txt          файл сборки
├── main.cpp                пример использования
├── printFramework.h        обеспеивает вывод структур данных на экран в удобном формате
├── README                  данная инструкция
└── simpleTestFramework.h   предоставляет функции дл удобного тестирования, использует printFramework

